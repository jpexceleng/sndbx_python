# comments
# ----------------------------------------------------------------------------
# This is a comment.

""" This 
    is
    a
    multi-line
    comment.
"""

# variables
# ----------------------------------------------------------------------------
# Variables are declared with assignment operator '=':
string = "Hello, world!"
print(string)

# Python is a "dynamically typed" language; means that there is no need for
# programmer to explicitly specify the data type before assigning a value to 
# a variable. The python interpreter automatically binds the type to its 
# variable.


# location in memory
# ----------------------------------------------------------------------------
# determine location of variable in memory with id() method:
print(id(string))